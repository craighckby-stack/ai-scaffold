name: Label Pull Request

on:
  pull_request_target:
    types: [labeled]

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - name: Label PR
        run: echo "PR labeled - automation handles this"
