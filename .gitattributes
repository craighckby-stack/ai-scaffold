# Default handling: auto-detect text files
* text=auto

# Force LF endings for common source/documentation files
*.{html,css,js,json,md} text eol=lf

# Force binary handling (images, documents, archives)
*.{png,jpg,jpeg,gif,ico} binary
*.{pdf,zip} binary
*.tar.gz binary

# Ignore macOS resource forks
__MACOSX/ binary